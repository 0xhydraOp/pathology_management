================================================================================
  IMPORTANT — READ THIS FIRST (Windows)
================================================================================

If you downloaded the **ZIP** named like:
    MONDAL DIAGNOSTIC CENTRE-<version>-Windows-Install-Package.zip

  1. Extract the ZIP (right-click → Extract All…).
  2. Inside the folder you will find:
       •  MONDAL DIAGNOSTIC CENTRE Setup <version>.exe   ← RUN THIS to install
       •  READ_ME_FIRST_Windows_Install.txt              (this guide)
  3. Double-click the **Setup .exe** — you should see the install wizard
     (license, choose folder, progress). That is a proper installation.
  4. After install, use the **Desktop** or **Start Menu** shortcut.

Do NOT look for a “portable” app .exe inside this ZIP — the package is only
the **installer** + instructions. There is no extract-and-run portable build.

If the program opens with NO wizard (no license, no “Installing…”):
  → You ran the wrong file, or an old portable zip from an older release.
  → Always run the file whose name contains **Setup** and ends in **.exe**.

================================================================================
AFTER INSTALL — WHERE IS MY DATA?
================================================================================
  • Program: folder you chose in Setup (often under Program Files).
  • Database, backups, exports:
        %APPDATA%\mondal-diagnostic-centre\
  • In the app: Settings → Support shows the exact path.

================================================================================
GitHub Releases / two assets
================================================================================
  You may see BOTH:
    • The same **Setup .exe** alone (direct download), AND
    • The **Windows-Install-Package.zip** (Setup + this readme together).

  Either way, run **Setup .exe** for a normal installation.

================================================================================
Build machine (developer)
================================================================================
  After:  npm run electron:build

  In release\ you should see:
    • MONDAL DIAGNOSTIC CENTRE Setup <version>.exe  (NSIS installer)
    • MONDAL DIAGNOSTIC CENTRE-<version>-Windows-Install-Package.zip
    • win-unpacked\  (intermediate; not for end users)

================================================================================
